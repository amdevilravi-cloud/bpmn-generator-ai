services:
  - type: web
    name: bpmn-generator-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app.main:app --workers=2 --bind=0.0.0.0:8000"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    autoDeploy: true
